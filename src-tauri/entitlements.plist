<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- Hardened runtime — required for notarization -->
    <key>com.apple.security.cs.allow-jit</key>
    <false/>

    <!-- Allow loading unsigned dylibs from the app bundle (WKWebView / Tauri) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- Allow running child processes with different signing (the container CLI) -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- Network client access — needed to reach registries, check for updates -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- Allow launching other apps (Terminal.app for shell access) -->
    <key>com.apple.security.temporary-exception.apple-events</key>
    <array>
        <string>com.apple.Terminal</string>
    </array>
</dict>
</plist>
